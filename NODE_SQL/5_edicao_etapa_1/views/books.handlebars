<h1>Meus livros:</h1>
<div class="books-container">
    <a href="/">Adicionar livro</a>
    {{#each books}}
    <div>
        # {{this.id}}
        -
        <span class="bold">{{this.title}}
            <a href="/books/{{this.id}}">{{this.title}}</a>
        </span>
        - 
        {{this.nm_pages}} páginas
    </div>
    {{/each}}
</div>